change from direct hydration file import to a mock path. ( Fix Cloudflare would not import the hydrate file when cloudflare-functions was used )

when ctrl is holded when navigating with a href the routing system fallbacks to the default beaviours
