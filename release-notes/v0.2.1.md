# Release Notes - v0.2.1

## Overview

This release fixes a build issue where non-React files were incorrectly processed by the built-in loader.

## 🐛 Bug Fixes

### Loader Bypass for Non-JSX Files

- **Fixed File Processing** - The built-in loader now correctly bypasses files that are not `.jsx` or `.tsx`, preventing incorrect transformations on other file types
- **Improved Compatibility** - Ensures that entrypoints, that is a non-React files are handled by their appropriate loaders

## 📝 Summary

Version 0.2.1 is a patch release that fixes loader handling for non-JSX/TSX files, improving build reliability when working with mixed file types in your project.
