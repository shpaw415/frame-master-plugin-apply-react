## 0.2.2 Release

---

### Windows compatibility Patch

- the path was broken when creating the routes, due to the colon beeing present in the absolute path ex: `C:`.
- loading virtual entrypoints absolute path was applyed to the route breaking the path on Win32 system.

---
